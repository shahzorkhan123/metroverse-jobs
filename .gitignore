# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/build
/build-serve
/.pnp
.pnp.js

# testing
/coverage

# production
/dist
dist.zip

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.env

npm-debug.log*
yarn-debug.log*
yarn-error.log*

*.pem
temp-deploy.txt

build.zip

# Downloaded data files (keep in data/raw/)
data/raw/*
.playwright-mcp/*

# Generated SQLite databases — rebuilt by pipeline, not committed directly.
# Commit the compressed .gz instead (see scripts/rebuild_analysis_db.py).
data/*.db
!data/*.db.gz

# Python
__pycache__/
*.pyc
.pytest_cache/

# Notebook figures (generated on execution — do not commit)
notebooks/.ipynb_checkpoints/